<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow" >
  <a class="navbar-brand" href="/">Pegasus</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      {{#if user}}
      
      <li class="nav-item active">
        <a class="nav-link" id="logout" href='/logout'>Logout</a>
      </li>
      {{#if user.privilege}}
        <a class="nav-link active" id="admin_panel" href='/admin_panel/'>Painel do Adiministrador</a>
        {{else}}
        <a class="nav-link active" id="manager_panel" href='/manager_panel/managepage'>Painel do Gestor</a>
      {{/if}}
      {{else}}
      <li class="nav-item active">
        <a class="nav-link" id="login" href='/login'>Entrar</a>
      </li>
      {{/if}}
    </ul>
  </div>
</nav>